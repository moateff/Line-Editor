# Line-Editor

A minimal interactive line editor for the Windows console using ANSI escape codes.  

## Features
- Insert characters at cursor  
- Backspace and Delete  
- Arrow keys (Left/Right)  
- Home and End  

## Usage
```cpp
char* text = lineEditor(x, y, capacity, startChar, endChar);
